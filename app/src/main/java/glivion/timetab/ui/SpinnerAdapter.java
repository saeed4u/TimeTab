package glivion.timetab.ui;


public class SpinnerAdapter// extends ArrayAdapter<String> 
{

/*	private Activity mContext;
	protected ArrayList<String> mData;

	public SpinnerAdapter(Activity context, int resource, ArrayList<String> data) {
		super(context, resource, data);
		this.mContext = context;
		this.mData = data;
	}

	@Override
	public View getView(int position, View convertView, ViewGroup parent) {
		return super.getView(position, convertView, parent);
	}

	@Override
	public View getDropDownView(int position, View convertView, ViewGroup parent) {
		View row = convertView;
		ViewHolder holder = new ViewHolder();
		if (row == null) {
			//LayoutInflater inflater = mContext.getLayoutInflater();
		//	row = inflater.inflate(R.layout.custom_spinner, parent, false);
		}
	//	holder.textView = (TextView) row.findViewById(R.id.bloodGroup);

		String bloodGroup = mData.get(position);
		if (bloodGroup != null) {
			holder.textView.setText(bloodGroup);
		}

		return row;
	}

	private static class ViewHolder {
		TextView textView;
	}*/
}
